[project]
name = "fedact"
version = "0.1.0"
description = "FedACT: Federated Action-Certified Threat Dynamics"
requires-python = ">=3.12"
dependencies = [
    "cvxpy>=1.9.2",
    "ecos>=2.0.14",
    "numpy>=1.26",
    "pandas>=3.0.5",
    "pydantic>=2.8",
    "pyyaml>=6.0",
    "scipy>=1.18.1",
    "torch>=2.3",
    "typer>=0.12",
]

[project.scripts]
fedact = "fedact.cli.main:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["fedact*"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "N"]

[tool.pyright]
include = ["src", "tests"]
extraPaths = ["src", "."]
venvPath = "."
venv = ".venv"
typeCheckingMode = "strict"
pythonVersion = "3.12"
reportMissingTypeStubs = false
reportUnknownMemberType = false

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "--import-mode=importlib -n auto"

[dependency-groups]
dev = [
    "nox>=2026.8.10",
    "vulture>=2.14",
    "deptry>=0.23",
    "pytest>=8.0",
    "pytest-xdist>=3.5",
    "ruff>=0.6",
    "pyright>=1.1",
    "types-pyyaml>=6.0",
    "import-linter>=2.13",
    "pytest-cov>=7.1.0",
]
[tool.deptry]
known_first_party = ["fedact"]

[tool.deptry.per_rule_ignores]
DEP004 = ["nox"]
DEP002 = ["types-pyyaml", "torch", "cvxpy", "ecos", "scipy", "pandas"]

[tool.ruff.lint.per-file-ignores]
"src/fedact/cli/main.py" = ["UP045"]

[tool.importlinter]
root_packages = ["fedact"]

[[tool.importlinter.contracts]]
name = "Layered package architecture"
type = "layers"
layers = [
    "fedact.cli",
    "fedact.app",
    "fedact.runtime",
    "fedact.experiments",
    "fedact.artifacts",
    "fedact.config",
    "fedact.domain",
]

[tool.coverage.run]
source = ["src/fedact"]

[tool.coverage.report]
fail_under = 85
show_missing = true

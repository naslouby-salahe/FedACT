# TODO remediation progress

- `2026-09-12` — `DISCOVERED`: completed repository-wide initial marker scan (221 production markers) and reviewed `src/fedact/domain/types.py`, existing enums, and imports/usages.
- `2026-09-12` — `ANALYZED`: identified the first shared remediation seam: executable workflow names and artifact path components are repeated across experiment modules.
- `2026-09-12` — `IMPLEMENTED`: remediated `data/lamda_apk_features.py:21,25,49-50,60-61,90,104-105,115,123,136-140`. The APK feature workflow now has a closed `LamdaFeatureCategory` enum and `AndroidManifestTag` enum, while API references, feature names, manifest values, and observed tokens retain separate semantic types across extraction and matching.
- `2026-09-12` — `REVIEWED`: removed the misleading enum-conversion markers from `domain/types.py`. Those aliases are open identifiers (not closed choice domains); their existing distinct semantic aliases are the correct type boundary.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED -> VERIFIED`: remediated `experiments/registry.py:21` and all nine original workflow/artifact markers in `experiments/robustness.py`. `experiment_directory` now requires `ExecutableWorkflowName`; its robustness callers pass the enum and use the new closed `WorkflowArtifactName` domain. Static compilation and marker rescan for these modules pass.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED -> VERIFIED`: remediated all eight original `analysis/comparisons.py:152-159` markers with the closed `SensitivityParameterName` enum, and all eight original `experiments/validation.py` workflow/artifact markers with canonical `ExecutableWorkflowName` and `WorkflowArtifactName` values. Static compilation and marker rescan for both modules pass.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED -> VERIFIED`: remediated `artifacts.py:86-88,163,167` with the canonical workflow artifact enum. Reviewed `data/synthetic.py:37-39`: grid and seed identities are generated/open values, so their existing distinct `NewType` boundaries (not enums) are correct; removed only the misleading markers.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED`: remediated all CLI command-name markers using `CliCommandName`. Typer receives the `StrEnum` at its external command-registration boundary; workflow names already remain typed as `ExecutableWorkflowName` end-to-end.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED -> VERIFIED`: remediated all five `data/ember2024.py` markers. EMBER JSON containers and week/month identities are open structured/external values and retain semantic `NewType` boundaries; control matching now preserves `WeekIdentifier | CalendarMonthCell` keys instead of converting them to raw strings. Static compilation and marker rescan pass.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED`: moved LAMDA feature-column selection into `configs/fedact.yaml` and the typed `LamdaPreprocessingRules` contract; production LAMDA population, identification, and preprocess workflows propagate that value into the data loader. Added configured raw-data and LAMDA-release directories to the workspace model and migrated the reviewed production call sites. The control-match index now retains `CalendarMonthString` keys.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED -> VERIFIED`: moved configuration payload/hash/key/mapping semantics to `domain/types.py`, preserving the YAML mapping boundary without leaking raw key sets. Generalized configuration membership validation over its actual domain value type and made field/path diagnostics semantic types. Production configuration successfully parses the updated YAML contract.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED -> VERIFIED`: remediated all five `data/clamav_signatures.py` markers. The fixed supplementary-file set and manifest filename are enums; manifest checksums, source, and acquisition time now carry semantic types through cache deserialization, download, and JSON serialization. Static compilation and marker rescan pass.
- `2026-09-12` — `IMPLEMENTED -> REVIEWED -> VERIFIED`: remediated all fourteen `data/lamda_apk_emulator.py` markers. Emulator handles, AVD/image/package/device identifiers, ports, event counts, seeds, timeout values, dynamic-result flags, observable events, and Jaccard output now preserve semantic types. The direct primitive-boundary scanner reports zero emulator violations.
